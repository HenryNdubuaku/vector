# Compiled output
/target/

# Cargo.lock — keep it for binaries/applications, ignore it for libraries
# Uncomment the next line only if this is a library crate
# Cargo.lock

# Backup files from rustfmt
**/*.rs.bk

# MSVC Windows debug files (harmless to keep on macOS)

*.pdb# macOS
.DS_Store

# Editor/IDE
.idea/
.vscode/
*.swp
*.lock
tests/cases/data/
