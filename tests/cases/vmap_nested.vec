function cell(c):
  c * c

function row(r):
  vmap(cell, r)

print(vmap(row, [[1.0, 2.0], [3.0, 4.0]]))

function rowsum(r):
  sum(vmap(cell, r))

print(vmap(rowsum, [[1.0, 2.0], [3.0, 4.0]]))

function aggn(m):
  sum(vmap(rowsum, m))

print(grad(aggn, [[1.0, 2.0], [3.0, 4.0]]))
